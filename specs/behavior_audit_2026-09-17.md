# 行为审计综合报告（2026-09-17，三智能体并行审计）

数据: reports/v06_aligned_full_48x3.json (144 轨迹) + specs/layer_op_probs_2026-09-17.json (13 轨迹概率族)。
原则（用户裁定）: 行为评估与答案脱钩——轨迹实际答案单独评估；证据概率 (p0/pF/d/l/f) 本身是**检索概率优势**，pF 高但答错 = 检索赢答案输（正检索信号），不是失败。

## 一、检索概率优势台账（问题 2 落实：pF 与 f1 分列）

| case | p0→pF | f1 | 读法 |
|---|---|---|---|
| 567_df97-s0 | .012→**.629** | 0 | **检索赢答案输**：gold 被主题证据支持，答案层用参数知识（"Bert I. Gordon 非 Ron Howard"）否决图证据，UNRESOLVED 当 CONTRADICTED，CASE-B 选错 |
| 2784-s2 | .032→**.548** | 0 | **检索赢答案输**：gold 就在 ?movie 绑定首位；R2 无解→answer NONE 对着环境打印的 bindings 重复 3 次原文分析 |
| 1379-s0/s1 | ≈0→≈0 | 0 | 检索也输（Priest 边从未出现）+ purity 折腾 |
| 1797-s1 | .151→.035 | 0 | 证据**降级**：20 实体巨中心淹没层 |
| 2209-s0 | .042→.314 | 0.11 | 多 gold 年份，部分选对（per-entity 修正后 pF 从 .095→.314） |
| 21-s0 / 2784-s0 / 626-s0 | .35→.86 / .03→.51 / .02→.41 | 1 | 双赢 |
| 1731-s0 | .003→.179 | 1 | pF 低但答对——gold 藏 CVT 属性内，打分器读不到（打分口径局限样本） |

结论：**检索很少是瓶颈**（95% 轨迹 gold 至少文本出现；两个 pF≥0.4/f1=0 标本 gold 都在手）——损失集中在答案层（拒答死锁、参数覆盖、UNRESOLVED≠CONTRADICTED、变量错位）。

## 二、标记术语表与设计漏洞（问题 1+3：措辞审查 + 冗余判定漏洞）

术语演化主线（详表见 SESSION_MEMORY 引用）：断链 walk-break(v2, 8 例)→v4 展示图基准下归零、由通路级 **unreach** 取代；台阶 lineage/替代对 substitute/冗余-重复 dup(N<0.2)/冗余-无关 irr(N≥0.2∧f≤τ)/不可缺 necessary/g 金标首达/f 前向独立/l 留一伤害/d 顺序增量(概率域)/N 新颖性(展示层)。测量基准在 展示→env ΔG→展示 间震荡过（§6.14→§6.20→§10.8→v4），且**术语不携带计算基准**。

### 设计漏洞（8 条，按严重度）

1. **redundant_irr 系统性误标有价值探索**（与"行为答案脱钩"原则直接冲突）。人机分歧 4/4：1379-s0-mod3（profession 新关系族探索，N=.48）、567_11fd-mod1（release_date 首查，**N=.90**）、1731-mod3（track 级保险探查）、2576-mod2（错误大假设下的连贯步骤，N=.82）全是新颖探索，f≈0 只因 gold 概率没动——**f 类标准是伪装的答案相关性**。dup（N≈0）标签可靠（restart 型真重取）。
2. **含金噪声不可记录**：567-[5] 三概率信号全负（l=−.028 移除反升）+通路 unreached，却因 g/台阶判 effective——档位无处携带 l<0 发现。需要复合标签（effective+noise-negative）。
3. **L1 短路静默救回 harmful 剖面**：1797-[5] d/f 双负 g=0 满足 harmful 三条件，但 必要∨台阶 短路 → effective，无冲突标记——判决取决于级联顺序而非调和语义。
4. **g 首现 vs 首取两裁定冲突**：L3 的 g-OR 臂不查 N——零新颖块经重渲染首现金标文本可拿满 effective，而 09-06 裁定（延迟声明≠功劳/drift 导入金标行）明确否决过此类。
5. **替代对满额信贷 vs 备份半额** 未再裁决：v1.5 替代对故意满额 vs 更早"备份=独立值一半/n 替代剩 1/n"——v4 的 146 条 substitute 通路继承满额，账未平。
6. **per-pathway 必要性对替代对双计**：两条替代通路各自"必要(删后断)"但可互换——pair-LOO（§6.16 已有）未接入通路判定（待裁定 3 仍在）。
7. **测量基准震荡不可见**：harmful-break 8→0 与 有效 69%→15% 两次翻转纯因换基准，术语不记录算在哪张图上。
8. **hub-gold φ 仅临时补丁**（v4 已结构性修复但待裁定 1/2 未关）。

### 人机冗余判定对照（6 标本）

dup 判定全对（1379-s1 mods4-5：restart 后全链重取，真冗余——**设计漏洞另计：restart 丢先验证据**）；irr 判定全错（见洞 1）。**冗余 vs 有价值的真正边界 = N（新颖性）与表面变体，不是 f**。

## 三、重复/头铁调用审计（问题 4）

总量：~69/1088 浪费调用（6.3%），高度集中——4 条烧满预算的轨迹（n_turns=17, 全 f1=0）= 1379 三样本 + 2784-s2。

### 量化

- **精确重复**：19 case / 25 次，仅 2 组被 repeat gate 拦（连续精确匹配才触发，非连续 22 组漏网）。
- **头铁**：检索侧干净（被拒后下一步 0 次原样重试——nudge 文本有效）；**真正的头铁在答案侧**——对着 `Second refusal`（环境把 bindings 原文打印出来）继续 `answer NONE`：2784-s2 连续 8 次、1379-s2 连续 7 次，直到预算耗尽，各浪费 15 回合。**最贵单一失败模式**。
- **表面变体绕过**：21 组/29 次（短/全关系名、名字/m.id、typed roster/?var）——同时绕过精确 gate 和 `_sg_served`（**语义幂等在 144 轨迹零命中——死机制**，名称解析变体破键）。
- **rr 改写风暴**：2 例（1379-s0 对 Franz Liszt 6 种问法，purity 第 4 次提醒后仍继续）。

### 环境机制评估

| 机制 | 接住 | 漏掉 | 成本 |
|---|---|---|---|
| repeat gate（连续精确） | 2 组，下一步即服从 | 非连续、全部表面变体、answer 豁免 | 命中时 1 回合，廉价有效 |
| purity-loop（rr 专属） | 改写风暴 | sg 重走；只键 center[0]；**闭包消息 off-by-one 且不 return 仍派发** | 提醒前烧 2 回合 |
| _sg_served 语义幂等 | **零命中** | 它为之设计的一切变体 | 死重 |
| refusal ladder | 首次拒答重定向 | 第二次起响应固定不升级 + answer 豁免 → 8-9 次重复到预算死 | 高达 8 回合 |

### 优化建议

**认知（提示语，SEQ_AGENTS_V21）**：
1. §7 停止教改写："② rephrase the question" 直接诱发 Franz-Liszt 风暴——改为"② submit the OTHER candidate_relations not yet tried；ranker 确定性——同 center 同列表，改问法不改变关系存在性"。
2. §11 echo-服从规则："环境 note 打印的 bindings 必须原样提交；NONE 仅当无变量有绑定时合法"（两例对着打印的答案重复 8 次）。
3. §0 表面等价句："短/全关系名、typed/m.id/?var 解析为同一游走——已服务的 (center, 关系集) 任何表面变体返回相同块；改关系集或层，不改表面形式"。
4. §0 `layer_action: repeat` 行动化："= 该层已交付，勿再调用——声明 checkpoint 或转下一 fact"。
5. §2.0 Stop Rule 同证据自检："我是否已用任何表面形式跑过该 (center, relations)？同游走=同结果=零新信息"。

**harness 快赢**：
6. 解析键 repeat gate（resolved center-set + short-rel-set 比较，非精确 json）——接住全部 21 组变体；代价：需在 gate 时做名字解析 + 豁免主动根重走（§0 合法语义）。
7. refusal 第三级升级：echo 后第 2 次相同拒答 → 自动提交打印的绑定集（或空）——最多省 8 回合，代价是提交可能不完整的答案，严格优于预算烧死的 None。
8. purity 闭包改 terminal（return 不派发）+ 修 off-by-one + 键扩展过 center[0]。

## 四、行为问题总清单（跨审计）

答案层：①拒答死锁（8 轨迹 ≥3 次 answer）②CASE-B 抛硬币（同证据 s0 对 s2 错——采样非推理）③参数知识覆盖图证据 ④UNRESOLVED≠CONTRADICTED ⑤答错变量（1923）⑥表面邻接实体（Twilight/New Moon 等 4 例）。
证据层：⑦巨中心淹没（1797）⑧CVT/id 不透明（日期成 m.xxx）。
判定层：⑨irr 误标探索（洞 1）⑩restart 丢证据致真重取（1379-s1）。
工程：⑪dispatch 崩溃 bug 2 次（dict append）。

修复杠杆排序：refusal 三级升级+echo 服从（提示语 2+机制 7）> 解析键 gate+表面等价教学（提示语 3+机制 6）> irr 判定改 N 边界（洞 1）> restart 证据保留。
